{
  "name": "arkan",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build app/src/app.ts --outfile app/app.js --target=browser --format=iife",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.18",
    "typescript": "^5.8.3"
  }
}
