[package]
name = "arkan"
version = "0.1.0"
edition = "2024"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
arkan-core = { path = "../crates/arkan-core" }
tauri = { version = "2", features = [] }

